server:
  port: 8089

spring:
  cloud:
    stream:
      bindings:
        messages-in-0:
          consumer:
            partitioned: true
          destination: messages
          group: messages_group
          content-type: application/json
      kafka:
        binder:
          brokers: localhost:9093
